// Utilizando for , crie um array que vá de 1 até 25 e imprima o resultado;
let numbers = [];
const length = 25;

for (let i = 1; i < length + 1; i += 1) {
  numbers.push(i);
}

console.log(numbers);
